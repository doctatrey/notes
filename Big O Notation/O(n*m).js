function printGrid(rows, cols) {
    for (let i = 0; i < rows; i++) { //runs 'n' times
        for (let j = 0; j < cols; j++) { // runs 'm' times
            console.log(`Cell (${i}, ${j})`)
        }
    }
}
